# 2026-07-08 记录

## Agent Market 健康巡检（09:00 cron）

- 脚本 `inspect_daily.py` 在 `CHAT_TEST=1 CHAT_TEST_ALL=1` 模式下运行
- 问题：Playwright 登录 phase 无输出、长时间卡住（Python 无缓冲输出问题，且 get_token() 每次都重新登录，不检查缓存 token）
- 前序 API-only 运行（无 chat test）已成功生成报告：`work/agent-market/reports/agent-health-report-20260708.md`
- 报告内容：41 个智能体，21 个对话型智能体测试（18 通过/3 异常）
- **飞书文档创建失败**：feishu_doc/create、feishu_drive/list、feishu_wiki/spaces 全部返回 400，飞书应用缺少 `docx:document`、`drive:drive:file:create` 等权限
- 替代方案：通过 message tool 直接发送摘要到 ou_12f4e5dbfd82f5975eaa6afd762b1d20

### 巡检关键发现

- 🔴 2 个智能体无使用指南（问学超级员工、客户信息查询小助手）
- 🔴 31 个智能体无用户评价
- 🟡 1 个零下载（问学超级员工）
- 🤖 对话测试 85% 通过率，3 个异常（权限/无回复）

### 需要修复

1. ✅ 脚本 `get_token()` 已添加缓存 token 有效性检查
2. ✅ 添加 `PYTHONUNBUFFERED=1` 或 `python3 -u` 确保实时输出
3. 飞书应用需添加 `docx:document` 和 `drive:drive:file:create` 权限以支持文档创建

## 15:00 后续修复 — Cron #16 投递失败与增强

### 问题诊断
- Cron #16 有 3 次连续错误（`consecutiveErrors: 3`），`lastDeliveryStatus: "not-delivered"`
- 根因：(1) isolated cron agent 尝试使用 `message` tool 发送飞书消息时崩溃；(2) 报告中的截图为本地路径，飞书群无法查看
- Cron prompt 已改为避免 `message` tool，使用 delivery announce 直接输出文本

### 已实施的修复（15:30）
- ✅ 添加 `import time` 并在 `run_chat_tests()` 中记录每个 Q&A 用时（`elapsed`）
- ✅ 每个智能体记录 `avg_elapsed`（平均用时）
- ✅ 详细报告和紧凑摘要中均显示 `⏱ 用时: Xs` / `⏱ 平均用时`
- ✅ `screenshot_to_base64_png()` — 截图转 base64 内嵌（已存在）
- ✅ `_embed_all_screenshots_in_report()` — 批量替换截图路径为 base64（已存在，已集成到 main()）
- ✅ `generate_compact_chat_summary()` — 生成紧凑文本摘要（纯文本 + 路径，无 base64 大块数据）
- ✅ `main()` 中调用 `_embed_all_screenshots_in_report()` 后处理报告
- ✅ `--stdout` 标志：将完整报告输出到 stdout
- ✅ 紧凑摘要保存为独立文件 `reports/agent-compact-summary-{date}.txt`
- ✅ 紧凑摘要也输出到 stdout

### 待完成
- 更新 cron #16 prompt 使用新格式
- 验证飞书群聊中 base64 图片是否可渲染
- 如果 base64 不可渲染，改用 `message` tool `attachments` 参数发送截图

### cron #16 当前配置
- ID: `25d841bb-d50a-426e-8146-cccabc97821c`
- Delivery: announce → feishu channel `oc_bef4f48fb4870602342af652e5501d86`
- Prompt: 已简化为「运行脚本 + cat 报告 + 不调 message 工具」
