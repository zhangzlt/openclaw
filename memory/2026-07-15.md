# 2026-07-15 每日笔记

## 巡检报告故障隔离修复

### feishu_build_doc_v2.py 重构
- **状态优先路由**：SKIPPED → BLOCKED → FAIL → PASS，先判断状态再校验字段
- **PASS 降级**：chat 类型 + answer_text 为空 → 自动降级 FAIL，标注"对话证据缺失"
- **逐 agent try/except**：单智能体异常不中断整份报告
- **图片块定位**：从 add_blocks 响应直接获取 block_id，不再依赖分页 get_all_blocks
- **本地块计数**：total_blocks 本地追踪，替代需分页的 get_children_count
- **code_block 兼容**：飞书 API 不支持 block_type=42，改为文本块包裹 markdown 代码围栏

### inspect_daily.py 新增功能
- **normalize_chat_evidence()**：多字段归一化（question_text / answer_text），支持所有字段别名
- **_bind_result 更新**：调用 normalize_chat_evidence，check answer_text 而非 agent_answer

### browser.py 重写 chat_wait
- 返回 dict（answer_text, status, stop_seen, stop_gone, waited）
- 新增 DOM 提取器：_extract_aily_answer、_extract_feishu_app_answer、_extract_generic_answer
- 排除用户问题、导航栏、按钮、输入框
- 等待 Stop generating 消失 + 连续 2 次稳定

### open_agent_ready 统一门禁
- 打开 agent URL → 检测页面类型（business / authorization / login / no_permission）
- authorization → 自动点击 Authorize → 等待跳转 → 确认
- login → 尝试自动登录 → 失败标记 BLOCKED
- business → 直接测试
- 截图前四重检测（auth / login / generating / stable）

## 真实验收

### Agent 76（神州问学知识库回答助手，Aily）
- URL：aily.feishu.cn/agents/agent_4jccvuk6yqb1y
- 点建议问题触发 → Deep Planning → 生成完成
- answer_text：2494 字完整回答（含 5 大章节，从"神州问学企业级Agent中台"开始）

### Agent 83（新海量采购系统智能助手，feishuapp）
- URL：bba12hub36.feishuapp.cn/ai/gui/chat/...
- 首次打开显示飞书授权页 → 自动点击 Authorize → 进入业务页
- 二次访问 cookie 有效 → 直接业务页
- answer_text："如您此前未申请过采购系统账号，请联系运维人员郭鑫（guoxinq）"

## Git 状态
- ✅ 已推送：947bf9d（故障隔离）、da643da（回答提取）
- ⚠️ 待推送：cc41f80（Agent 76 验收）、89af27f（open_agent_ready + Agent 83）
- 推送失败原因：GitHub HTTP/2 流中断，已配 HTTP/1.1，网络恢复后执行 `git push origin master`

## 飞书平台适配器
- Aily：aily.feishu.cn → [class*="ThreadMessage-module__assistant"]
- feishuapp：feishuapp.cn → chatContainer / chatMainContainer → 去除 welcome 消息
- 通用降级：innerText diff 排除 UI 元素

## 飞书原生代码块支持（block_type=14）

### 背景
之前用 text_block 包裹 Markdown 代码围栏（```...```），飞书不解 Markdown，显示为普通文字 + 反引号。
且回答被拆成单字换行（"\n".join(string) 对字符串逐字分割的 bug）。

### 解决方案
- **native_code_block()**: 使用 block_type=14，language=1 (Plain Text)
- **关键发现**: 飞书 API 会截断 text_run.content 中第一个 `\n` 之后的内容
  解决方案：每行一个 text_run，行间插入 `content="\n"` 的 text_run
- **normalize_block_content()**: 防单字拆分，处理 str/list/dict/None 四种输入
  - 字符串：原样返回 strip()
  - 列表："\n".join 拼接各元素文本
  - 字典：取 text/content/answer/value 字段
  - None：返回空字符串

### 报告生成逻辑
- 对话型 PASS：文本标签（"测试操作："）+ 原生代码块（question_text）+ 文本标签（"测试结果："）+ 原生代码块（answer_text）
- 失败不静默回退，逐智能体隔离
- 最终验证新增：对话 PASS 检查 ≥2 个 block_type=14

### Agent 101 验证
- 文档 ID: Zmt7dZCMcoIRvfxTACmcg5i6nre → ZbGhdS3BGoJfksxP3dQcHk0XnPf
- block 结构: 2 个 type=14 代码块，内容无 ``` 或 """，无单字换行，\n 完整保留

## Git 推送状态
- ✅ 全部已推送（HTTP/1.1 修复推送问题）
- 新 commit: aca2cc5（原生代码块 + normalize_block_content + 最终验证）

## Agent 115 & 106 专项调试（2026-07-15 12:36）

### Agent 115（DI问答助手）— chat_wait 提前结束

**根因**：
- Aily 平台流式回答不显示 "Stop generating"（在 body.innerText 中不可见）
- `_extract_aily_answer` 用 DOM class 选择器（.assistant/.ai-message），Aily 用原生 HTML（h1/p/table/li）渲染回答
- 唯一能提取到的是标题文本 14 chars："神州数码BI账号申请流程指南"

**修复（browser.py）**：
1. `chat_wait` 重构：内容稳定性为主信号
   - 不再依赖 "Stop generating" 出现/消失
   - 内容非空 + 连续 stable_count 次不变 → 完成
   - 最长 180s，超时保留答案快照
2. 新增 `_extract_aily_answer_body`：在 body.innerText 中按"用户问题 → How was this result?"区间截取

**验证**：955 字符完整回答，含账号申请入口、客户端安装配置、术语说明表、注意事项

### Agent 106（职场文案速写·全能版）— chat_send 未执行

**根因（双重）**：
1. **_detect_chat_from_page 假阴性**：Aily 测试模式 UI 关键词（"Chat directly here to test your agent" / "Message your agent" / "Suggested questions"）不在检测列表中 → 判定为非对话型 → 跳过 chat_send
2. **ace-line contenteditable 发送失败**：Aily 用 ace-line 编辑器（`<div class="ace-line"><span>text</span></div>`），`insert_text` 后尾部残留换行 → Enter = 创建新 ace-line（换行）而非发送

**修复**：
1. `_detect_chat_from_page`（inspect_daily.py）：新增 3 个关键词
2. `chat_send`（browser.py）：检测到 ace-line 后清空 DOM 子节点 → 重建单个干净 ace-line → 光标移到末尾 → Enter 发送
3. `_find_send_button`：新增 `button.input-icon-button` / `button.btn-pc` 选择器

**验证**：
- 推荐问题触发 → 成功导航到 conversation_4km2gj627f0k6 → AI 追问 392c
- 自定义问题 → 清理输入框 → type → Enter → 发送成功 → AI 二轮回答 392c

### 完整修复文件
| 文件 | 修改内容 |
|------|---------|
| `browser.py` | `chat_wait` 内容稳定性主信号；`_extract_aily_answer_body` body 截取；`chat_send` ace-line 清理；`_find_send_button` Aily 选择器 |
| `inspect_daily.py` | `_detect_chat_from_page` 新增 Aily 关键词 |
| `debug_agent_115.py` | 新增：115 诊断脚本 |
| `debug_agent_106.py` | 新增：106 发送门禁诊断脚本 |


## agent-browser state/profile 互斥修复（2026-07-15 16:00）

### 根因
agent-browser 0.31.1 CLI 禁止 `--state` 和 `AGENT_BROWSER_PROFILE` 同时使用，错误：
`✗ Cannot use storage_state with profile` exit=1

触发条件：`playwright_state.json` 和 `feishu-browser-profile/` 目录同时存在时，`_agent_browser_auth_kwargs()` 同时传入两者 → 全局 41 个智能体全部失败。

### 修复
- **`_agent_browser_auth_kwargs()`** (inspect_daily.py)：state 优先（`playwright_state.json` 含有效 Cookie，profile 目录 Cookie 已过期）
  - 新增 Cookie 有效性检测：检查 profile 的 `Cookies` 文件是否存在且修改时间 > 1h
  - state 存在 → 只传 state；profile 可用 → 只传 profile
  - 两者均可用 → state 优先（已验证可靠）
- **`AgentBrowser.__init__` 断言**（browser.py）：禁止 state_path + profile_path 同时传入
- **CRON_PROFILE_PATH 配置化**：可通过环境变量覆盖默认 profile 路径

### 错误分类新增 browser_infrastructure
- 浏览器级别异常（timeout/browser/chrome/session/connection/singleton/profile/devtools）→ `browser_infrastructure`（🔧图标）
- 不再标为 `chat_error`
- 连续 3 次 `browser_infrastructure` → 触发熔断，放弃剩余智能体巡检

### SingletonLock 清理
- 异常退出残留 SingletonLock → 启动时自动检测并删除
- 清理逻辑：`AgentBrowser.__init__` 中检查 profile_path 下的 `SingletonLock`

### 验证
- 单元测试 5/5 全部通过（互斥断言、auth_kwargs、Cookie 有效性、熔断计数器、状态分类）
- 烟雾测试：Agent 76 + Agent 83 均正常打开并提取回答
- cron Run #39 触发 → 报告正常产出验证

### Git
- Commit: `96a8c17` → `dce0912`（含 Run #39 报告产出）
- 远程: `origin/master` 已同步
- 推送问题：`--force-with-lease` 超时 → 改用普通 `git push` 成功
